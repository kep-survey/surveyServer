server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    password: 1234
    url: jdbc:mariadb://localhost:3306/test_db
    username: kep
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true